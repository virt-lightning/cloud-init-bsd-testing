---
image_os: openbsd
image_version: 6.6
final_name: "{{ image_os }}-{{ image_version }}"
base:
  distro: openbsd-6
dependencies:
    - coreutils
    - bash
    - gsed
    - flock
    - bash
    - git
builder_repo: https://github.com/goneri/pcib
