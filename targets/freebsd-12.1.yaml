---
image_os: freebsd
image_version: 12.1
final_name: "{{ image_os }}-{{ image_version }}"
base:
  distro: freebsd-12
dependencies:
    - qemu-utils
    - curl
    - bash
    - git
    - ca_root_nss
builder_repo: https://github.com/virt-lightning/freebsd-cloud-images
